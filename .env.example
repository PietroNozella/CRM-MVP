NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key

# Server-only (nunca use prefixo NEXT_PUBLIC_). Usado em rotas API / server actions.
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key

# Segredo do webhook de leads. Envie como Authorization: Bearer <segredo>.
# Obrigatorio: sem ele a captura responde 503. Dev local: defina qualquer valor.
LEADS_WEBHOOK_SECRET=gere-um-segredo-forte

# Marca do cliente (opcional, padrao "CRM"). Ver src/lib/site.ts.
NEXT_PUBLIC_APP_NAME=CRM

# Mensagem do botao Chamar no WhatsApp, use {nome} (opcional).
# NEXT_PUBLIC_WHATSAPP_TEMPLATE=Olá {nome}, obrigado pelo contato! Como posso ajudar?
